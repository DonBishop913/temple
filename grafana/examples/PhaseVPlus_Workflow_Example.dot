digraph PhaseVPlusWorkflow {
    rankdir=LR;
    node [shape=box, style=filled, color=lightblue];

    JoyParticle -> Aggregator -> ReplayEngine;
    EmpathyBridge -> Aggregator;
    Codex -> Aggregator;
    Faithseed -> Aggregator;
    ABTLogs -> Aggregator;

    ReplayEngine -> WebSocketClient -> FlowReplayPanel;
    FlowReplayPanel -> Annotations;
    Annotations -> CodexArchive;
}
